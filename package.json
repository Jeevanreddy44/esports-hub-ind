{
  "name": "esports-hub-india",
  "version": "1.0.0",
  "description": "Full-stack Esports Hub (Vercel Frontend + Railway Backend)",
  "scripts": {
    "install:backend": "npm install --prefix backend",
    "install:frontend": "npm install --prefix frontend",
    "build:frontend": "cd frontend && npm install && npm run build && cd .. && node scripts/deploy-sync.js",
    "build": "npm run build:frontend",
    "start": "npm run install:backend && node backend/server.js",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "engines": {
    "node": "20.x"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.103.3",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.3",
    "jsonwebtoken": "^9.0.2",
    "node-fetch": "^2.7.0",
    "pg": "^8.20.0"
  }
}
